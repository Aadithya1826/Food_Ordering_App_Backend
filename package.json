{
  "name": "restaurant-backend",
  "version": "1.0.0",
  "description": "Python FastAPI Backend for Restaurant Mobile App",
  "scripts": {
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload",
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload"
  },
  "author": "",
  "license": "ISC"
}
